import ExternalButton from 'apps/device/views/single-calendar-2/actions-bar/Button'

import styled from 'styled-components'

const Button = styled(ExternalButton)`
  font-size: 0.8rem;
  padding: 0.2rem 0.9rem;
`

export default Button